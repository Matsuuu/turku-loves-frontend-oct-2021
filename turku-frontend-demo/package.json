{
  "name": "turku-frontend-demo",
  "description": "Webcomponent turku-frontend-demo following open-wc recommendations",
  "license": "MIT",
  "author": "turku-frontend-demo",
  "version": "0.0.0",
  "scripts": {
    "start": "tsc && concurrently -k -r \"tsc --watch --preserveWatchOutput\" \"wds\""
  },
  "dependencies": {
    "lit": "^2.0.0-rc.2"
  },
  "devDependencies": {
    "@web/dev-server": "^0.1.21",
    "concurrently": "^5.3.0",
    "tslib": "^2.3.1",
    "typescript": "^4.3.5"
  }
}
